import {ReactElement} from 'react';
// import firebase from 'firebase';
// import {useAppContext} from '_hooks';

export const AddNet = (): ReactElement => {
  /*const context = useAppContext();
  const {user} = context;
  firebase.firestore().collection("nets").doc("LA").set({
    managers: [user.uid],
    products: [],
    owner: user.uid,
  })*/

  return (
    <div>
      
    </div>
  );
};

export default AddNet;
